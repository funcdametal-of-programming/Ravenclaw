#include<iostream>
#include<cmath>

using namespace std;
int main()
{
int option,decimal,num,i=0,base,remainder,converted[100],j;

char hexadecimalNumber[100];//array to hold the hexadecimal resultant


cout <<"\t\t\t\t_____welcome_____\n";
cout <<"select an option \n";
cout << "1. Convert decimal to any base\n";
cout << "2.convert binary to decimal\n";
cout << "Enter your choice: ";
cin  >> option;

switch(option){
//to convert decimal to any base
case 1:
  cout <<"enter number: ";
  cin >>num;
 if(isDecimal==true){
    cout <<"enter the base you want to change into: ";
    cin >>base;
    int i = 0,j;
   if(base==16){
     //repeatedly divide the number by 16 and store the remainder in hexadecimalNumber. 
            while(num>0){
             int remainder = num % 16;
               // number converted to its ASCII equivalent
        if (remainder < 10)
            hexadecimalNumber[i] = remainder + 48;
        else
            hexadecimalNumber[i] = remainder + 55;
        num = num/ 16;
        i++;
            }
            // prints the hexadecimal equivalent of the input number by iterating through the hexadecimalNumber array in reverse order and printing each element.
        for (int j = i - 1; j >= 0; j--)
    cout<<hexadecimalNumber[j];
    }
    else{while (num > 0) {
        converted[i] = num % base;
        num/= base;
        i++;
    }
        for (int j = i - 1; j >= 0; j--)
        cout << converted[j];
    }
          }
   else{
    cout<<"invalid input";
}
   break;

//to convert binary to decimal
case 2:
  cout <<"enter number: ";
  cin >>num;
 while (num != 0) {
        remainder = num % 10;
        num /= 10;
        decimal += remainder * pow(2, i);
        i++;}
        cout <<decimal;
    break;
default :
    cout <<"invalid input";

}}
